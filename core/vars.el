(defvar emacs-dir "~/.emacs.d/"
  "Root of emacs configuration")

(defvar core-dir (concat emacs-dir "core/")
  "Directory where core functionalities of this configuration goes")

(defvar vendor-dir (concat emacs-dir "vendor/")
  "Directory where all packages goes")

(provide 'vars)
